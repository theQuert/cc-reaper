{"task_id": "1.1", "description": "resource-watch snapshot", "files": ["shell/resource-watch.sh"], "acceptance": ["one invocation appends exactly one log line with ts/load/cpu-idle/mem/disk fields", "completes <2s, no loop/daemon"], "depends_on": []}
{"task_id": "1.2", "description": "thresholds + notify + cooldown", "files": ["shell/resource-watch.sh"], "acceptance": ["breach posts osascript notification and ALERT log marker", "no breach => log only", "same-metric repeat within cooldown is silent", "different metrics alert independently", "CC_RW_* env overrides defaults"], "depends_on": ["1.1"]}
{"task_id": "1.3", "description": "resource-watch plist", "files": ["launchd/com.cc-reaper.resource-watch.plist"], "acceptance": ["StartInterval 600", "Nice and LowPriorityIO set", "plutil -lint passes"], "depends_on": []}
{"task_id": "1.4", "description": "resource-watch tests", "files": ["tests/resource-watch.sh"], "acceptance": ["stubbed snapshot scenario passes", "cooldown suppression scenario passes", "all tests exit 0"], "depends_on": ["1.2"]}
{"task_id": "2.1", "description": "disk-janitor --check", "files": ["shell/disk-janitor.sh"], "acceptance": ["check mode deletes nothing (no rm/tmutil delete/docker prune executed)", "detects snapshot-pin (snapshots present + disk<threshold) and logs/alerts"], "depends_on": []}
{"task_id": "2.2", "description": "disk-janitor --clean", "files": ["shell/disk-janitor.sh"], "acceptance": ["each unavailable tool logs SKIP and continues", "docker prune never includes --volumes (grep-able assertion)", "snapshot thinning only targets com.apple.TimeMachine.* and only when disk<threshold", "per-target freed bytes logged"], "depends_on": ["2.1"]}
{"task_id": "2.3", "description": "disk plists", "files": ["launchd/com.cc-reaper.disk-check.plist", "launchd/com.cc-reaper.weekly-clean.plist"], "acceptance": ["disk-check StartInterval 3600 runs --check", "weekly StartCalendarInterval Sunday 04:00 runs --clean", "both Nice+LowPriorityIO, plutil -lint passes"], "depends_on": []}
{"task_id": "2.4", "description": "disk-janitor tests", "files": ["tests/disk-janitor.sh"], "acceptance": ["check-readonly scenario passes with stubs", "forbidden --volumes assertion passes", "thinning gating scenario passes", "all tests exit 0"], "depends_on": ["2.2"]}
{"task_id": "3.1", "description": "worktree inventory + gates", "files": ["shell/worktree-janitor.sh"], "acceptance": ["lists every non-primary worktree with dirty/branch/ahead/push/active fields", "dirty>0 => KEEP", "active cwd => KEEP", "lsof failure => KEEP (conservative)"], "depends_on": []}
{"task_id": "3.2", "description": "report + --apply removal", "files": ["shell/worktree-janitor.sh"], "acceptance": ["no flags => report only, nothing removed", "--apply removes only REMOVABLE via git worktree remove with force-fallback for ignored-only residue", "summary shows removed/reclaimed/kept+reasons"], "depends_on": ["3.1"]}
{"task_id": "3.3", "description": "worktree-janitor tests", "files": ["tests/worktree-janitor.sh"], "acceptance": ["fixture repo: dirty-keep passes", "clean-remove passes", "apply-gating (default no delete) passes", "all tests exit 0"], "depends_on": ["3.2"]}
{"task_id": "4.1", "description": "install.sh integration", "files": ["install.sh"], "acceptance": ["installs 3 plists via launchctl bootstrap/load", "creates ~/.cc-reaper/{logs,state}", "update path replaces existing plists idempotently"], "depends_on": ["1.3", "2.3"]}
{"task_id": "4.2", "description": "docs", "files": ["README.md", "CHANGELOG.md"], "acceptance": ["README documents 3 scripts + schedules + safety boundaries", "CHANGELOG entry added"], "depends_on": ["4.1"]}
{"task_id": "4.3", "description": "live smoke", "files": [], "acceptance": ["plists loaded and first snapshot log line appears", "disk-janitor --check runs clean on live machine", "worktree-janitor report matches live repos without error"], "depends_on": ["4.1"]}
